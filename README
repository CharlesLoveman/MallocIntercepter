To build the library run 'make'.
Before executing a program using LD_PRELOAD, you will need to set $LD_LIBRARY_PATH to include the directory containing malloc.so.
Running 'make run' will execute seq with the library preloaded as in the example.
